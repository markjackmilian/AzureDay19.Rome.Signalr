<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:markupExtensions="clr-namespace:Xam.Zero.MarkupExtensions;assembly=Xam.Zero"
             xmlns:home="clr-namespace:AzureDay.Rome.Xam.ViewModels;assembly=AzureDay.Rome.Xam"
             x:Name="Gyroscope"
             BindingContext="{markupExtensions:ShellPagedViewModelMarkup ViewModel={x:Type home:GyroscopeViewModel}, Page={x:Reference Gyroscope}}"
             x:Class="AzureDay.Rome.Xam.GyroscopePage">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <BoxView 
            HeightRequest="30" 
            WidthRequest="30"
            TranslationX="{Binding Left}"
            TranslationY="{Binding Top}"
            BackgroundColor="Red"
            VerticalOptions="Start" 
            HorizontalOptions="Start"/>
        
        <StackLayout Grid.Row="1">
        
            <Button 
                Text="{Binding IsGyroscopeActive, Converter={StaticResource IsActiveToTestConverter}}" 
                Command="{Binding ToggleGyroscopeCommand}"/>
            
        </StackLayout>
    </Grid>

</ContentPage>